services:
  clipforge:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - path: .env
        required: false
    volumes:
      - clipforge-data:/data
      - whisper-cache:/root/.cache/huggingface
    restart: unless-stopped
volumes:
  clipforge-data:
  whisper-cache:
